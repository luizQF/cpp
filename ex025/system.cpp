#include <iostream>
#include <cstdlib>
using namespace std;
int main(){
    system("dir");//diretorio do arquivo
    system("cls"); //limpa o prompt
    system("pause");// pausa o prompt caso seja executado fora do compilador
    system("color F8");// cores do prompt em hexadecimal, usar color/? para ver as cores
    return 0;
}